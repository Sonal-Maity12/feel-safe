// Defining the Contact type
export interface Contact {
    name: string;
    phoneNumber: string;
    email?: string; // Optional field, can be omitted
    avatar?: string; // Optional field for avatar URL or image path
    // Add any other fields relevant to your contact data
  }
  